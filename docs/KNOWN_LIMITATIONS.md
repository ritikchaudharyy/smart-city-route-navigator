# Known Limitations

- The bundled city data is a simplified example graph and is not a real-world map.
- The application uses a simple text-based graph format rather than a formal database or API-backed storage layer.
- The current UI is a desktop Swing application and does not yet support live traffic or GPS integration.
- The application is single-threaded and intended for local desktop use.
