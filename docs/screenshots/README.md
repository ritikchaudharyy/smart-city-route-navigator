# Screenshots

Add application screenshots here before publishing to GitHub, e.g.:

- `main-window.png` — the main window with a route found and highlighted on the map
- `splash-screen.png` — the startup splash screen
- `about-dialog.png` — Help > About
- `settings-dialog.png` — View > Settings

The root `README.md` references these filenames directly, so keeping the
same names means no other file needs to change once images are added.
